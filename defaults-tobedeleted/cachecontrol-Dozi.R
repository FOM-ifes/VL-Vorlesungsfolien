#
# Cache Control (Dozi)
#
knitr::opts_chunk$set(
    progress = TRUE,
    verbose = TRUE,
    cache = 1,
    cache.lazy = FALSE,
    autodep = TRUE,
    cache.path = '__cache_dozi/' # Gemeinsames CACHE Verzeichnis für schnellere Massenproduktion!
)