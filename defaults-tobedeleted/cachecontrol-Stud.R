#
# Cache Control (Studi)
#
knitr::opts_chunk$set(
    progress = TRUE,
    verbose = TRUE,
    cache = 1,
    cache.lazy = FALSE,
    autodep = TRUE,
    cache.path = '__cache_studi/' # Gemeinsames CACHE Verzeichnis für schnellere Massenproduktion!
)